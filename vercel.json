{
  "buildCommand": "npm run build",
  "outputDirectory": "apps/web/dist",
  "monorepo": true,
  "ignoreCommand": "git diff --quiet HEAD~1 HEAD -- apps/web",
  "env": {
    "NODE_ENV": "production"
  }
}
