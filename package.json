{
  "name": "athleticap",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter web dev & pnpm --filter api dev",
    "build": "pnpm --filter db build && pnpm --filter web build && pnpm --filter api build",
    "db:push": "pnpm --filter db push",
    "db:seed": "pnpm --filter db seed"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^25.9.1",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "prisma": "^5.22.0",
    "tsx": "^4.22.3"
  }
}
